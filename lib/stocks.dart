import 'package:flutter/material.dart';

class StocksPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gestion des stocks'),
      ),
      body: Center(
        child: Text('Page de gestion des stocks'),
      ),
    );
  }
}
